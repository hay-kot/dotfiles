---
dev_dir: "{{ user_home }}/code"

dev_containers_postgres_user: "postgres"
dev_containers_postgres_password: "postgres"
dev_containers_postgres_port: "5432"

dev_containers_mysql_user: "mysql"
dev_containers_mysql_password: "mysql"
dev_containers_mysql_port: "3306"

dev_containers_mongo_user: "mongo"
dev_containers_mongo_password: "mongo"
dev_containers_mongo_port: "27017"
dev_containers_mongo_express_port: "8081"

git_username: "Hayden"
git_email: "64056131+hay-kot@users.noreply.github.com"
git_global_ignore_file: "{{ user_home }}/.gitignore_global"
git_global_ignore_content: |
  # An example global gitignore file
  #
  # Place a copy if this at ~/.gitignore_global
  # Run `git config --global core.excludesfile ~/.gitignore_global`

  # Compiled source #
  ###################
  *.com
  *.class
  *.dll
  *.exe
  *.o
  *.so
  *.pyc
  *.pyo

  # Packages #
  ############
  # it's better to unpack these files and commit the raw source
  # git has its own built in compression methods
  *.7z
  *.dmg
  *.gz
  *.iso
  *.jar
  *.rar
  *.tar
  *.zip
  *.msi

  # Logs and databases #
  ######################
  *.log
  *.sql
  *.sqlite

  # OS generated files #
  ######################
  .DS_Store
  .DS_Store?
  ._*
  .Spotlight-V100
  .Trashes
  ehthumbs.db
  Thumbs.db
  desktop.ini

  # Temporary files #
  ###################
  *.bak
  *.swp
  *.swo
  *~
  *#
