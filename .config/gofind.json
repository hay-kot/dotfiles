{
  "default": "repos",
  "commands": {
    "docker": {
      "root": "~/docker",
      "match": "docker-compose*"
    },
    "repos": {
      "root": "~/Code",
      "match": ".git"
    }
  },
  "cache": "~/.cache/gofind/"
}
